# Аудит базы DnD — Санитарка
*Дата: 2026-03-07*

## Битые ссылки

### В индексе, но файл не существует
Проверены индексы:
- `npcs/_index.md`
- `quests/_index.md`
- `locations/_index.md`

Результат: **не найдено** ссылок на отсутствующие файлы.

### Файлы без записи в индексе

Найдены файлы, существующие на диске, но отсутствующие в индексах:

- `quests/beekeeper.md`
- `quests/find-ilkai.md`
- `quests/find-magic-academy.md`
- `quests/prizrak-ogon.md`
- `quests/save-rbak.md`
- `quests/statya-irvin.md`
- `quests/temple-library.md`

По `npcs/` и `locations/`: файлов без записи в индексах не обнаружено.

## Проблемы YAML frontmatter

Проверены все `.md` в:
- `npcs/`
- `quests/`
- `locations/`
- `characters/`

Обязательные поля: `name`, `type`, `status`.

Файлы без frontmatter:
- `npcs/_index.md`
- `quests/_index.md`
- `locations/_index.md`
- `characters/_party.md`

Файлов с frontmatter, но с неполным набором обязательных полей: **не найдено**.

## Дубликаты и несоответствия имён

По slug/name в `npcs/` и `quests/` явных дубликатов (близких пар файлов) не найдено.

Выявлено терминологическое расхождение по названию сущности:
- `Алигар` встречается в:
  - `npcs/abraham-kelken.md`
  - `quests/airship-escape.md`
- `Олигар` / `Олигары` встречается в:
  - `npcs/rbak.md`
  - `quests/airship-escape.md`

Вывод: в базе одновременно используются обе формы (`Алигар*` и `Олигар*`), унификация не завершена.

## Расхождения статусов квестов

Сверены статусы из `quests/_index.md` со `status` в frontmatter файлов квестов, которые присутствуют в индексе.

Результат: **расхождений не обнаружено**.

## Итог
- Критичных проблем: **8**
  - 7 квестовых файлов вне `quests/_index.md`
  - 1 несоответствие именования (`Алигар*`/`Олигар*`)
- Рекомендации:
  1. Добавить все неиндексированные квесты в `quests/_index.md` (или архивировать/удалить, если они неактуальны).
  2. Принять единый канон написания (`Алигар*` или `Олигар*`) и привести `npcs/` + `quests/` к одной форме.
  3. Явно определить политику для служебных файлов (`_index.md`, `_party.md`):
     - либо не требовать frontmatter от таких файлов,
     - либо добавить служебный frontmatter для унификации валидаторов.
